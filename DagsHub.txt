# DagsHub integration link (example)
MLFLOW_DAGSHUB_URI=https://dagshub.com/ProfDARA/membangun_model

# Quick usage (PowerShell) - replace values with your own:
# $env:DAGSHUB_OWNER = "your-username-or-org"
# $env:DAGSHUB_REPO  = "your-repo"
# $env:DAGSHUB_TOKEN = "PASTE_YOUR_DAGSHUB_TOKEN_HERE"
# Optional: set DAGSHUB_USER if your username differs from owner
# $env:DAGSHUB_USER  = "your-username"

# Then run the training script (it will build MLflow URI automatically):
# python train_with_mlflow_dagshub.py --dagshub_owner $env:DAGSHUB_OWNER --dagshub_repo $env:DAGSHUB_REPO --experiment "Amazon_Daily_Revenue_Forecasting_Advanced"

# Or explicitly pass a tracking URI (less secure if embedding token in URL):
# python train_with_mlflow_dagshub.py --tracking_uri "https://<user>:<token>@dagshub.com/<owner>/<repo>.mlflow"

# Notes:
# - If you don't provide DAGSHUB_TOKEN, the script will fallback to the provided
#   --tracking_uri value (default is local sqlite:///mlruns.db).
# - Keep your token secret. Avoid committing tokens into git history.
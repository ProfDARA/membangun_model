# Nama Dataset / Preprocessing

Letakkan file dataset hasil preprocessing di folder ini atau beri nama file sesuai yang dipanggil oleh skrip.

Expected filenames (used by `modelling.py` / `modelling_tuning.py`):
- `daily_sales_forecasting.csv`

If you have raw data instead, add a short script to convert it into
`daily_sales_forecasting.csv` and place it here.

Replace this README with your actual preprocessed CSV or a script.